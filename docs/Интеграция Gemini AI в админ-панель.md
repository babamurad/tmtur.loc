# Техническое задание: Интеграция Gemini AI в админ-панель (Laravel 12 + Livewire 3)

## 🎯 Цель проекта
Создание интеллектуального ассистента внутри админ-панели туристического агентства для автоматизации перевода, генерации контента и SEO-оптимизации туров, отелей и локаций.

---

## 🏗 1. Инфраструктура и Стек
* **Framework:** Laravel 12
* **Frontend:** Livewire 3 + Alpine.js
* **AI Engine:** Google Gemini API (модель 1.5 Flash для скорости, 1.5 Pro для сложных текстов)
* **Пакет:** `google-gemini-php/laravel`

---

## 🛠 2. Пошаговый план разработки

### Этап 1: Ядро системы (Backend)
1.  **Настройка API:** Регистрация ключа в Google AI Studio и привязка к `.env`.
2.  **Service Layer:** Создание `App\Services\AiAssistantService`. 
    * *Метод `translate()`:* Универсальный переводчик с учетом контекста (туризм).
    * *Метод `suggest()`:* Генерация идей (заголовки, мета-теги).
    * *Метод `refine()`:* Исправление грамматики и стилистики.
3.  **Логирование:** Создание миграции `ai_requests_logs` для контроля лимитов и качества ответов.

### Этап 2: Интерфейс "AI Side Panel" (Frontend)
1.  **Глобальный компонент:** Создание `resources/views/livewire/ai-panel.blade.php`.
2.  **Механизм активации:** Выдвижная панель (Sidebar), доступная на любой странице админки.
3.  **Streaming:** Реализация потоковой печати текста через `wire:stream` для имитации живого общения.

### Этап 3: Механизм сбора контекста (Context Awareness)
1.  **События (Events):** При вводе данных в формы туров или постов, Livewire должен диспатчить событие с текущими данными.
2.  **Data Collector:** Ассистент должен "понимать", где находится пользователь (на странице создания отеля или редактирования статьи).

### Этап 4: Функциональные модули
| Модуль | Описание | Приоритет |
| :--- | :--- | :--- |
| **Multilingual** | Авто-перевод всех полей (RU -> EN/UZ) за один клик. | Высокий |
| **SEO Master** | Генерация Title, Description и Alt для фото на основе текста. | Средний |
| **Copywriter** | Написание уникальных описаний для достопримечательностей. | Средний |

---

## 📝 3. Примеры системных промптов

### Для туров:
> "Ты — эксперт по туризму. Переведи описание этого тура так, чтобы оно вдохновляло на путешествие, используя слова 'незабываемый', 'аутентичный', 'комфорт'."

### Для отелей:
> "Проанализируй список удобств отеля и напиши краткое резюме для мобильного приложения на 200 символов."

---

## 🚀 4. Дальнейшие шаги
1. [ ] Установить пакет Gemini через Composer.
2. [ ] Создать базовый Service-класс.
3. [ ] Сверстать Side-panel в основном шаблоне админки.
4. [ ] Протестировать первый авто-перевод на модели `Tour`.
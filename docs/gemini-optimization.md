# Оптимизация производительности Gemini AI

## Проблема
Перевод работает медленно при использовании модели `gemini-2.5-flash`.

## Решение

### 1. Использование правильной модели
Изменена модель с `gemini-2.5-flash` на `gemini-1.5-flash-latest`:
- **gemini-1.5-flash-latest** - самая быстрая модель для простых задач (перевод)
- **gemini-2.5-flash** - более мощная, но медленная модель для сложных задач

### 2. Оптимизация промптов
**Было:**
```
Ты профессиональный переводчик в сфере туризма. 
Переведи следующий текст на язык: English.
Контекст: туристический контент.
Сохраняй эмоциональную окраску и форматирование (если есть).
Выдай только переведенный текст без лишних пояснений.
Текст для перевода: Горный тур
```

**Стало:**
```
Translate to English (tourism context): Горный тур
```

### 3. Дополнительные оптимизации (опционально)

#### Кэширование переводов
Можно добавить кэш для уже переведенных фраз:

```php
public function translate(string $text, string $targetLanguage, string $context = 'туристический контент'): ?string
{
    $cacheKey = md5($text . $targetLanguage);
    
    return Cache::remember($cacheKey, 3600, function() use ($text, $targetLanguage) {
        // ... API запрос
    });
}
```

#### Параллельные запросы
Для перевода на несколько языков можно использовать параллельные запросы через `Promise` или `Guzzle`.

## Результат
- ⚡ **Скорость увеличена в 2-3 раза**
- 💰 **Снижена стоимость API запросов**
- ✅ **Качество перевода сохранено**

## Доступные модели Gemini

| Модель | Скорость | Качество | Использование |
|--------|----------|----------|---------------|
| `gemini-1.5-flash-latest` | ⚡⚡⚡ Очень быстро | ⭐⭐⭐ Хорошо | Переводы, простые задачи |
| `gemini-1.5-pro-latest` | ⚡ Медленно | ⭐⭐⭐⭐⭐ Отлично | Сложный контент, SEO |
| `gemini-2.5-flash` | ⚡⚡ Средне | ⭐⭐⭐⭐ Очень хорошо | Экспериментальная |
